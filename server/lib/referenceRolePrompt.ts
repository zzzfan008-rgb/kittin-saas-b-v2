/**
 * Backwards-compatible server import. The implementation lives in src so
 * browser admission, evaluation evidence, and Provider runtime share one
 * renderer contract.
 */
export { referenceRolePrompt } from "../../src/lib/providerPromptRenderer";
