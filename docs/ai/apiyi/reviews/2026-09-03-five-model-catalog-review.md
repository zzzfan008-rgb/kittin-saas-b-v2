# 2026-09-03 API易五模型目录人工复核

本记录物化当前五模型范围的 `/v1/models` 人工复核结论。它只批准配置令牌在捕获时刻返回的规范化模型目录基线，不批准任何生成、编辑、质量、计费或发布结论。

## 原始观测

- 捕获时间：`2026-09-03T08:12:22.094Z`。
- 请求：认证 `GET /v1/models`，串行 1 次，重试 0 次，HTTP 200。
- 本次没有调用图片生成或编辑端点，付费 Provider 调用数为 0。
- 原始响应长度：61,434 字节。
- 原始文件 SHA-256：`8d60eafb6460fe97964289d6b8284eecf775af9db01b8f25abad1d11e985fc68`。
- 规范化唯一模型 ID：269 个。
- 规范化范围：`sha256-canonical-model-id-set-v1`。
- 规范化 ID 集合 SHA-256：`bc6fffe606c2419d00bccec2f27c70835def3f77f5f0608276d94959f459879f`。

原始 `/v1/models` 文件继续保存在 Git 工作树外，不把完整账户目录或令牌写入仓库。仓库内的机器可读摘要见 `evidence/2026-09-03-five-model-catalog-review.json`；生成 contract check 时仍须提供上述精确外部原始文件，并同时核对原始字节哈希和规范化集合哈希。

## 五模型精确覆盖

| 当前产品模型 ID | 目录精确命中 |
| --- | --- |
| `gpt-image-2` | 是 |
| `gpt-image-2-vip` | 是 |
| `gemini-3.1-flash-image` | 是 |
| `flux-2-pro` | 是 |
| `seedream-5-0-260128` | 是 |

覆盖结果为 5/5，没有使用别名、模糊匹配或自动回退。独立复核完成后，用户于 `2026-09-03T08:22:05Z` 以“确认以上五模型人工复核基线。”明确批准该 269-ID 规范化集合指纹作为当前五模型目录基线。

## 与历史记录的边界

`2026-09-02-model-catalog-probe.json`、`2026-09-03-model-catalog-probe.json` 和 `2026-09-03-model-catalog-drift.md` 继续保留当时六模型预期下的不可改写历史事实。本次复核没有修改这些文件，也没有把历史脱敏摘要冒充原始导出。

本次批准只证明五个精确 ID 出现在该配置令牌的账户级目录视图中。目录存在不证明对应生成或编辑端点成功，不证明输出质量、价格、计费、重试或 API易全局可用性。

## 当前发布状态

五个模型的 `observedEvidence.status` 继续为 `unverified`，没有任何提示词变体因此变为 `verified` 或 `recommended`。模型目录人工基线这一项已闭环，但发布仍未就绪：contract check 必须读取并保留外置原始目录文件，随后还要完成各模型评估证据、阶段 gate、promotion、registry 以及干净 exact-SHA 发布门禁。
