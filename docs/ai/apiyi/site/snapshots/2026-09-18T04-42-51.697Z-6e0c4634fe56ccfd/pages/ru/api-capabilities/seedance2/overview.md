> ## Documentation Index
> Fetch the complete documentation index at: https://docs.apiyi.com/llms.txt
> Use this file to discover all available pages before exploring further.

# Генерация видео Seedance 2.0 / 2.5

> ByteDance Seedance через официальные ресурсы Volcengine: новый 2.5, а также 2.0 в вариантах standard / быстрый / Mini — преобразование текста в видео, первый+последний/первый кадр, мультимодальные референсы, редактирование и расширение видео. Для каждого соотношения сторон действует одинаковая цена на соответствующем уровне, синхронизированный звук включён по умолчанию. Версия 2.5 добавляет 30-секундные клипы, 30 референсных изображений и вывод в формате mov; тарификация выполняется в той же группе SeeDance2, что и для семейства 2.0.

## Обзор

**doubao-seedance-2-5-260628** (2.5), **doubao-seedance-2-0-260128** (стандартная), **doubao-seedance-2-0-fast-260128** (быстрая) и **doubao-seedance-2-0-mini-260615** (мини/лайт) — новейшее семейство моделей генерации видео от ByteDance: четыре модели, работающие параллельно и предоставляемые через APIYI на **официальных ресурсах Volcengine в материковом Китае** (не международная версия BytePlus), со встроенной upstream-проверкой безопасности контента. Они поддерживают преобразование текста в видео, преобразование первого+последнего/первого кадра в видео и преобразование мультимодальных референсов в видео, а также могут генерировать голос, звуковые эффекты и фоновую музыку, синхронизированные с визуальным рядом.

**2.5 — наиболее производительный уровень**: максимальная длительность увеличена с 15 до **30 секунд**, количество референсных изображений — с 9 до **30**, аудио может использоваться как самостоятельный референс, а также добавлены вывод в формате **mov** и явные типы задач редактирования/продления видео. **Она также стоит дороже** — примерно в 1,5 раза больше, чем стандартная модель 2.0 (около \$1.35 против \$0.91 за 720p/5 с), что соответствует разнице между двумя поколениями в собственных прайс-листах Volcengine. Семейство 2.0 остаётся доступным и не выводится из эксплуатации: для обычных клипов до 15 секунд стандартная модель дешевле и **также поддерживает 1080p**; mini — выбор для массового производства (**примерно вдвое дешевле стандартной модели за единицу и генерирует быстрее**, с ограничением до 720p), а fast занимает промежуточное положение.

**2.5 работает в той же группе `SeeDance2`, что и семейство 2.0** (0.18x) — один token обеспечивает доступ ко всем четырём моделям. См. «Настройка группы» ниже.

<Note>
  **🎬 Основные возможности**: 2.5 поддерживает **4–30 с**, семейство 2.0 — 4–15 с (оба принимают `-1` для выбора длительности моделью); три уровня разрешения (480p/720p/1080p, при этом **1080p доступно только для 2.5 и стандартной 2.0**); 6 соотношений сторон плюс адаптивное; **синхронизированное аудио включено по умолчанию**; многоязычные промпты. Создано для масштабного **производства коротких видео, материалов для электронной коммерции, моушн-дизайна и контента с виртуальными персонажами**.
</Note>

<Info>
  **🔥 Ограниченная по времени скидка — до 2026-10-07 23:59 (UTC+8)**: две новые группы для отдельных моделей, `SD2Mini` (0.10x) и `SD2Fast` (0.15x), снижают тариф на **44.4% для `mini` и на 16.7% для `fast`**. Просто используйте один новый Token — изменения кода не требуются. См. «Группы с ограниченной по времени скидкой» и «Настройка группы» ниже.
</Info>

<Warning>
  **Для вызова 2.5 токенам требуется группа `SeeDance2`** (коэффициент тарифа 0.18x) — та же группа, что и для семейства 2.0. Вызов `doubao-seedance-2-5-260628` токеном из группы Default или другой видеогруппы завершится ошибкой «**no available channel for this model**».

  **Один Token с включённым `SeeDance2` обеспечивает доступ ко всем четырём моделям** — 2.5 и семейство 2.0 используют одну группу и один тариф, поэтому в вашем коде меняется только поле `model`.
</Warning>

<CardGroup cols={2}>
  <Card title="Справочник API генерации видео" icon="video" href="/ru/api-capabilities/seedance2/video-generation">
    `POST /seedance/api/v3/contents/generations/tasks` — эндпоинт асинхронных задач с интерактивной Playground и полным кодом для опроса/скачивания.
  </Card>

  <Card title="Руководство по API" icon="book-open" href="/ru/api-manual">
    Создание токенов, базовый URL, модели тарификации и общие правила вызовов.
  </Card>

  <Card title="Визуальное тестирование API" icon="flask-conical" href="https://icover.ai/seedance-official">
    Отлаживайте этот эндпоинт непосредственно в инструменте визуального тестирования iCover — код не требуется.
  </Card>

  <Card title="Поиск / скачивание асинхронных задач" icon="list-checks" href="https://api.apiyi.com/task">
    Просматривайте отправленные задачи генерации видео и скачивайте ссылки на видео в консоли APIYI — запись поиска вне API.
  </Card>
</CardGroup>

## Позвольте AI-агенту выполнить интеграцию

<Note>
  Если вы разрабатываете с помощью Codex / Claude Code / Cursor, скопируйте приведённый ниже промпт и передайте его своему агенту. Сначала он получает текстовую версию этой страницы (добавьте `.md` к любому URL документации), а затем пишет код в используемом в вашем проекте стеке — асинхронный опрос, **24-часовое истечение срока действия ссылки, требующее немедленного копирования**, ловушка с gzip-заголовком и критические ограничения параметров уже включены в требования.
</Note>

<Prompt description="Поручите агенту для написания кода интегрировать или устранить неполадки в генерации видео Seedance 2.5 / 2.0. Скопируйте и вставьте этот текст в Codex, Claude Code, Cursor и аналогичные инструменты." icon="bot" actions={["copy"]}>
  Интегрируйте или устраните неполадки в генерации видео Seedance 2.5 / 2.0 (генерация видео из текста, первый/последний кадр, мультимодальные референсы, редактирование видео, расширение видео) в этом проекте.

  Прочитайте документацию до изменения кода: получите текстовую версию страницы по адресу [https://docs.apiyi.com/en/api-capabilities/seedance2/overview.md](https://docs.apiyi.com/en/api-capabilities/seedance2/overview.md). Подробности об эндпоинте см. на странице генерации видео, а правила для ресурсов — на страницах библиотеки ресурсов и референсов ресурсов; ко всем URL добавляйте тот же суффикс `.md`.

  Требования:

  1. Используйте трёхэтапный асинхронный поток, а не синхронное ожидание. Отправьте запрос на `POST /seedance/api/v3/contents/generations/tasks`, чтобы получить ID задачи, опрашивайте `GET /seedance/api/v3/contents/generations/tasks/{id}`, а после успешного завершения скачайте видео. Здесь легко ошибиться в трёх местах: **префикс пути — `/seedance/api/v3`; не удаляйте `/api` и не используйте универсальные эндпоинты видео `/v1/videos`, `/v1/video/generations` или `/v2/videos/generations`** (почему — см. в разделе «Эндпоинты API»); ID задачи находится в поле верхнего уровня **`id`**, а не в `task_id`; и **статус успешного завершения — `succeeded`, а не `completed`** (машина последовательно переходит в состояния `queued`, затем `running`, а потом `succeeded` / `failed` / `expired`). Интервалы опроса: после отправки подождите от 20 до 30 секунд перед первым запросом, затем выполняйте опрос каждые 10–20 секунд; общий бюджет времени — 15 минут.

  2. Получение видео: адрес находится в **`content.video_url`** — внутри `content`, а не на верхнем уровне. Это подписанная ссылка, которая **истекает через 24 часа**, поэтому **немедленно скачайте видео на стороне сервера и разместите его в собственном объектном хранилище или CDN**; никогда не сохраняйте её в базе данных как долгосрочный адрес. **Не отправляйте заголовок `Authorization`** при получении видео по этой ссылке. Сам ID задачи остаётся доступным для запросов в течение 7 дней.

  3. **Отправляйте заголовок `Accept-Encoding: identity`**, если используете requests в Python. Шлюз помечает ответ как `content-encoding: gzip`, хотя тело фактически не сжато, поэтому без этого заголовка возникают случайные ошибки `ContentDecodingError`, усечённый JSON (например, пропадает начальный `{"`) или необъяснимые ошибки 400. curl и fetch в браузере не затрагиваются — проблема возникает только у клиентов с автоматической распаковкой, таких как requests.

  4. Ключевые параметры: сначала выберите модель — `doubao-seedance-2-5-260628` (2.5, рекомендуется) или `doubao-seedance-2-0-260128` / `-fast-260128` / `-mini-260615`. `resolution` принимает значения **в нижнем регистре** `480p` / `720p` / `1080p`, по умолчанию используется `720p`; **только 2.5 и стандартная 2.0 поддерживают `1080p`** (fast и mini возвращают ошибку 400), а **ни одна модель этого семейства не поддерживает `4k`**. `ratio` принимает одно из семи значений (`16:9`, `4:3`, `1:1`, `3:4`, `9:16`, `21:9`, `adaptive`); по умолчанию используется `adaptive`, а любое значение за пределами этого набора (например, `2:1`) возвращает ошибку 400. `duration`: **для 2.5 это целое число от 4 до 30, а значение по умолчанию — `-1`** — если не указать параметр, модель сама выберет длительность (при тестировании использовалось 10 секунд, что удваивает стоимость ролика длительностью 5 секунд, поэтому **явно передавайте этот параметр, если стоимость важна**); для семейства 2.0 диапазон составляет от 4 до 15, значение по умолчанию — 5. **Для `generate_audio` по умолчанию используется `true`**, поэтому для вывода без звука необходимо явно передать `false`. Наконец, `frames` и `camera_fixed` относятся к Seedance 1.x и здесь не поддерживаются — не отправляйте их.

  5. Референсные изображения и ресурсы: изображения помещаются в `content[]` в виде элементов `{"type":"image_url", ...}`; их значением может быть общедоступный URL, URI данных Base64 или ID из библиотеки ресурсов (`asset://...`). Режимы **взаимоисключающие**: первый/последний кадр (2 изображения с обязательным `role`, равным `first_frame` или `last_frame`), только первый кадр (1 изображение) или мультимодальный референс (**2.5 принимает до 30 изображений, 10 видео и 10 аудиоклипов, причём аудио можно использовать отдельно**; семейство 2.0 принимает 9 изображений, 3 видео и 3 аудиоклипа и требует как минимум одного изображения или видео вместе с аудио). Размер каждого изображения не должен превышать 30 МБ; длина его сторон должна составлять от 300 до 6000 пикселей, а соотношение сторон — от 0,4 до 2,5. **Изображения с лицами реальных людей отклоняются предварительной модерацией** — такие изображения сначала необходимо зарегистрировать в библиотеке ресурсов, а затем указать как `asset://`, при этом в промпте следует называть их по позиции: изображение 1, изображение 2 и так далее. **Никогда не записывайте необработанный ID ресурса в промпт.**

  6. **Два жёстких ограничения, существующих только в 2.5; их нарушение сразу возвращает ошибку 400**: для задач с первым кадром / первым и последним кадром, редактированием видео и расширением видео параметр `ratio` **должен иметь значение `adaptive`** — конкретное соотношение сторон будет отклонено; а для редактирования видео параметр `duration` **должен иметь значение `-1`**, при этом исходное видео должно длиться от 4 до 30 секунд. Для редактирования или расширения явно передавайте `omni_reference_task_type` (`edit` / `extend` / `auto`), чтобы недопустимые параметры отклонялись синхронно при отправке с ошибкой `InvalidParameter.TaskTypeConstraint`, а не приводили к сбою задачи через несколько минут. 2.5 также принимает `output_format` (по умолчанию `mp4` или `mov`); mov — это формат с высокой точностью цветопередачи для профессионального постпродакшна, который некоторые проигрыватели не могут открыть.

  7. Тарификация и ошибки: тарификация выполняется на основе token и определяется **площадью разрешения, умноженной на длительность**, поэтому все соотношения сторон при одинаковом разрешении стоят одинаково. **Одно видео создаёт в журналах две строки тарификации** (резервирование при отправке, затем сверка после завершения) — это нормально; фактическая стоимость равна их сумме, и это не двойное списание. Ошибки 400, вызванные параметрами, не тарифицируются, а ошибки с префиксом `PUBLIC_` являются блокировками предварительной модерации; они также не тарифицируются, и после изменения ресурса или промпта запрос можно повторить. Ошибка о том, что для модели недоступен канал, означает, что указана неверная token-группа — **2.5 и семейство 2.0 используют группу `SeeDance2`** (для `mini` / `fast` также доступны льготные `SD2Mini` / `SD2Fast`), с режимом тарификации с оплатой по мере использования или PAYG-priority. При сбое генерации, в том числе из-за модерации, **не повторяйте многократно запрос с идентичным промптом**.

  8. Получайте ключ из переменной окружения `APIYI_API_KEY`. Никогда не встраивайте его в код и не добавляйте в git.

  9. Завершив работу, действительно выполните один вызов генерации видео из текста и один вызов генерации видео из изображения с первым кадром, затем покажите мне видео и стоимость каждого из этих двух вызовов. Учтите, что весь процесс занимает несколько минут; при запуске в среде с ограничениями увеличьте тайм-аут команды свыше 600 секунд или запустите её в фоновом режиме.
</Prompt>

<Accordion title="От каких проблем защищает этот промпт">
  | Требование                                             | Предотвращаемая ошибка                                                                                                                                              |
  | ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
  | Статус успешного завершения — `succeeded`              | Перенос логики из другой модели видео: вы естественным образом указываете `completed`, а затем бесконечно выполняете опрос, так и не дожидаясь успешного завершения |
  | ID задачи — это `id` верхнего уровня                   | Он называется не `task_id`, а адрес видео находится внутри `content.video_url`, а не на верхнем уровне                                                              |
  | Отправляйте `Accept-Encoding: identity`                | Gzip-заголовок шлюза противоречит содержимому тела, поэтому requests в Python случайным образом выдаёт ошибки декодирования или обрезает JSON                       |
  | Скачивайте и сразу размещайте в собственном хранилище  | Ссылка истекает через 24 часа, а отправка `Authorization` во время скачивания фактически нарушает её работу                                                         |
  | Две строки тарификации — это нормально                 | Резервирование плюс списание, а не двойная тарификация; фактическая стоимость равна их сумме                                                                        |
  | Реальные лица обрабатываются через библиотеку ресурсов | Отправка фотографии реального человека напрямую отклоняется модерацией; сначала зарегистрируйте её, а затем укажите как `asset://`                                  |
  | В 2.5 `duration` по умолчанию принимает значение `-1`  | Если не указать длительность, модель сама выберет её (при тестировании — 10 с), что удваивает стоимость                                                             |
  | В 2.5 три типа задач требуют `ratio: adaptive`         | Первый/последний кадр, редактирование и расширение отклоняют любое конкретное соотношение сторон с ошибкой 400                                                      |
</Accordion>

## Почему Seedance от APIYI?

Сначала уточним позиционирование: эта модель **не имеет официальной скидки, и APIYI не устанавливает на неё цену с целью получения прибыли** — она предлагается, чтобы **обеспечить доступность ресурсов и обслуживать клиентов**. Реальная ценность использования APIYI заключается не в «дешевизне», а в доступе и удобстве работы:

<CardGroup cols={2}>
  <Card title="Официальный ресурс · Материковая версия" icon="shield-check">
    Официальные ресурсы Volcengine для материкового Китая (а не международная версия BytePlus) со встроенной проверкой безопасности контента на стороне upstream. Параметры, ответы и тарификация полностью соответствуют официальному API.
  </Card>

  <Card title="Доступ к белому списку виртуальных лиц" icon="scan-face">
    Канал предоставляет доступ к **белому списку виртуальных лиц** на стороне upstream: лица, созданные ИИ, и виртуальные аватары можно напрямую использовать для преобразования изображения в видео; отдельная заявка на включение в белый список официального канала не требуется (лица реальных людей по-прежнему ограничены проверкой безопасности контента на стороне upstream).
  </Card>

  <Card title="Библиотека ресурсов включена бесплатно" icon="images">
    [Приватная библиотека ресурсов](/ru/api-capabilities/seedance2/asset-library), лежащая в основе видео с сохранением идентичности персонажа (загрузка виртуального аватара + проверка реального человека), **бесплатна в APIYI**. В официальном сервисе это отдельно приобретаемое дополнение — годовой контракт стоимостью в несколько сотен тысяч юаней для клиентов без рамочного соглашения. Мы включаем его в стоимость API.
  </Card>

  <Card title="Цены с приоритетом доступности ресурсов · На уровне официальных" icon="percent">
    Официальной скидки не существует, и APIYI не получает прибыль от этой модели: цены за единицу соответствуют официальному прайс-листу Volcengine (тарификация на платформе примерно на 10% выше); с учётом [бонусов за пополнение](/ru/faq/recharge-promotions) фактическая стоимость **примерно соответствует стоимости официального канала**, а клиенты с крупными пополнениями могут получить цену ниже официальной на некоторых уровнях.
  </Card>

  <Card title="Неограниченные параллельные запросы · Без очередей" icon="infinity">
    В наших тестах все 15 одновременных задач сразу поступили в `running` без ожидания в очереди (измерено 2026-06-06 (UTC+8)) — сервис готов к масштабному пакетному производству.
  </Card>

  <Card title="Доступ без лишних препятствий · Без проверки личности" icon="globe">
    **Не требуется аккаунт Volcengine, проверка настоящего имени или личности, а также достижение минимального порога расходов** (не нужно вносить активационный депозит в размере 200 юаней и проходить проверку предприятия). Центры обработки данных материкового Китая, домашние сети и зарубежные узлы могут напрямую обращаться к `api.apiyi.com` с помощью одного Token.
  </Card>

  <Card title="Полная линейка видеомоделей" icon="layers">
    [VEO 3.1](/ru/api-capabilities/veo-3-1-official/overview) и [Wan2.7](/ru/api-capabilities/wan/overview) доступны на одной платформе — комбинируйте их в зависимости от задачи.
  </Card>

  <Card title="Профессиональная поддержка" icon="handshake">
    Команда с опытом работы с задачами генерации видео предоставляет поддержку в выборе моделей, настройке и интеграции — от PoC до производственной эксплуатации.
  </Card>
</CardGroup>

## Ключевые возможности

<CardGroup cols={2}>
  <Card title="Три уровня · Одинаковая цена внутри уровня" icon="monitor">
    480p / 720p / 1080p (**1080p только для standard 2.5 и 2.0**; для fast и mini максимум — 720p). Внутри уровня 16:9, 9:16, 1:1 и любое другое соотношение сторон имеют **одинаковую площадь в пикселях и одинаковую цену** — переключайтесь между альбомной и портретной ориентацией без доплаты.
  </Card>

  <Card title="Синхронизированный звук по умолчанию" icon="volume-2">
    `generate_audio` по умолчанию имеет значение true: голос, звуковые эффекты и фоновая музыка генерируются в соответствии с визуальным рядом. Заключайте произносимые реплики в двойные кавычки, чтобы повысить качество озвучивания.
  </Card>

  <Card title="Управляемая длительность до 30 с" icon="timer">
    2.5 принимает целые секунды от **4 до 30**, семейство 2.0 — от 4 до 15; `-1` позволяет модели выбрать длительность (тарификация по фактическому результату). **В 2.5 значение `duration` по умолчанию — `-1`** — не указывайте его, и модель выберет за вас. Фиксированные 24 fps.
  </Card>

  <Card title="Многоязычные промпты" icon="languages">
    Китайский (до \~500 символов) и английский (до \~1000 слов), а также японский, испанский, португальский и индонезийский.
  </Card>
</CardGroup>

<CardGroup cols={2}>
  <Card title="Первый+последний / первый кадр" icon="image">
    Закрепите первый и последний кадры с помощью двух изображений или анимируйте одно изображение как первый кадр. Используйте вместе с `return_last_frame`, чтобы объединять клипы в более длинные непрерывные видео.
  </Card>

  <Card title="Мультимодальная генерация видео по референсам" icon="images">
    2.5 принимает до **30 изображений + 10 видео + 10 аудио**, при этом аудио можно использовать отдельно; семейство 2.0 принимает 9 изображений + 3 видео + 3 аудио, при этом для аудио требуется сопутствующее изображение или видео. Создавайте, редактируйте или расширяйте видео, сохраняя согласованность персонажей и стиля.
  </Card>

  <Card title="Асинхронный поток задач" icon="clock">
    Отправьте запрос и получите `task_id`, опрашивайте статус, затем скачайте mp4 из `content.video_url` (ссылка действует 24 часа).
  </Card>

  <Card title="Воспроизводимые seeds" icon="dices">
    Зафиксируйте `seed` для похожих результатов между запусками. Значение `watermark` по умолчанию — false — результат не содержит водяного знака.
  </Card>
</CardGroup>

## Тарификация

<Info>
  **Тарификация в одной строке — точное списание по token, уровень за уровнем привязанное к официальному сайту Volcengine.** Для четырёх моделей действуют **разные цены**: mini \< fast \< standard \< **2.5** (в том же порядке, что и на официальном сайте; mini работает примерно по половине удельной цены standard, а 2.5 — примерно по цене в 1,5 раза выше неё — **это НЕ один и тот же ценовой уровень**). Volcengine **не предоставляет скидок** на эту серию, а данный канал работает как **чистый официальный релей для гарантированного доступа к поставкам**, а не ради прибыли. Цены рассчитываются относительно базового уровня «клиент с крупным депозитом и бонусом 20%»: номинальное списание составляет 1,26× официальной цены (коэффициент тарифа 0,18 × фиксированный курс обмена 1:7), что после применения [бонуса за пополнение](/ru/faq/recharge-promotions) в размере 20% даёт **примерно 1,05× официальной цены** — эта надбавка в 5% в основном покрывает расходы на НДС в размере 6% при оформлении специальных счетов-фактур. Для обычных клиентов с бонусом 10% итог составляет примерно 1,15×. Тарификация выполняется по площади×длительности, поэтому **отклонение в пределах ±5% является нормальным** — вы можете протестировать, сверить расчёты и связаться с нами в любое время.

  Помимо удельной цены, этот канал предоставляет ещё два преимущества: **аккаунты из белого списка могут генерировать настоящие человеческие лица** (по умолчанию это официально заблокировано), а [библиотека ресурсов](/ru/api-capabilities/seedance2/asset-library), необходимая для видео с единообразным отображением персонажей, **бесплатно включена в APIYI** (официально это отдельное платное дополнение — годовой контракт стоимостью в сотни тысяч CNY для клиентов без рамочного соглашения). Ни одно из этих преимуществ не отражено в приведённом выше сравнении удельных цен.
</Info>

Тарификация на основе token: `tokens ≈ (input video duration + output duration)(s) × output width × output height × 24 / 1024` (длительность входного видео равна 0 для преобразования текста/изображения в видео; в наших тестах это подтверждено с точностью до 0,1%). Поскольку каждый коэффициент внутри уровня имеет одинаковую площадь в пикселях, **цена зависит только от уровня разрешения, длительности результата и наличия видео во входных данных**.

### Официальные ценовые ориентиры (формат 16:9 / выход 5 с, CNY за видео)

**① Без входного видео** (text-to-video / image-to-video / референсные изображения):

| Разрешение | Стандартная `doubao-seedance-2.0` | Быстрая           | Mini              |
| ---------- | --------------------------------- | ----------------- | ----------------- |
| 480p       | CNY 2.31                          | CNY 1.86          | CNY 1.16          |
| 720p       | CNY 4.97                          | CNY 4.00          | CNY 2.50          |
| 1080p      | CNY 12.39                         | Не поддерживается | Не поддерживается |

**② С входным видео** (мультимодальный референс, включая `video_url`; входное видео 2–15 с, нижний диапазон ≈ 2–4 с входного видео, верхний диапазон ≈ 15 с входного видео):

| Разрешение | Стандартная `doubao-seedance-2.0` | Быстрая           | Mini              |
| ---------- | --------------------------------- | ----------------- | ----------------- |
| 480p       | CNY 2.53 - 5.62                   | CNY 1.99 - 4.42   | CNY 1.28 - 2.84   |
| 720p       | CNY 5.44 - 12.10                  | CNY 4.28 - 9.50   | CNY 2.74 - 6.10   |
| 1080p      | CNY 13.56 - 30.13                 | Не поддерживается | Не поддерживается |

<Note>
  **Каждая цена в таблице ② по-прежнему рассчитана для 5-секундного выхода**; диапазон отражает только длительность входного видео (2–15 с) — CNY 30.13 означает «5 с выхода + 15 с входного видео», **а не выход длительностью 15 секунд**. При использовании входного видео тарифицируемая длительность = **длительность входного видео + длительность выхода**, причём обе части преобразуются в кадры с выходным разрешением; также применяется минимальный порог tokens (очень короткие входные видео тарифицируются по этому порогу). Авторитетным источником данных об использовании является возвращаемый `usage.completion_tokens`.

  **Пример расчёта** (стандартная модель, 1080p, выход 12 с + референсное видео 12 с): для 1080p используется 1920 × 1080 ÷ 1024 = 2025 tokens на кадр; выход: 24 × 12 + 1 = 289 кадров, плюс 288 входных кадров = 577 × 2025 = 1,168,425 tokens. При официальной ставке для входного видео около CNY 0.031 за 1K tokens это составляет примерно **CNY 36.2**; номинальная стоимость в APIYI — \$6.52 (CNY 45.6), а для клиента с крупным пополнением после бонуса 20% — около CNY 38.0. Эта задача не соответствует ни одной ячейке таблицы ②, поскольку выход не равен 5 секундам.
</Note>

**Сравнение, измеренное на платформе** (тестирование проведено в 2026-06 и 2026-07, формат 16:9 / аудио по умолчанию / без входного видео; CNY по фиксированному курсу 1:7, только для справки):

| Модель                            | Разрешение | Длительность | Стоимость APIYI | CNY    | Общая цена ÷1.1 (¥) | Для крупного клиента ÷1.2 (¥) | Официальный ориентир (¥) |
| --------------------------------- | ---------- | ------------ | --------------- | ------ | ------------------- | ----------------------------- | ------------------------ |
| `doubao-seedance-2-0-fast-260128` | 720p       | 5 с          | \$0.7253        | ¥5.08  | ¥4.62               | ¥4.23                         | ¥4.00                    |
| `doubao-seedance-2-0-fast-260128` | 480p       | 5 с          | \$0.3373        | ¥2.36  | ¥2.15               | ¥1.97                         | ¥1.86                    |
| `doubao-seedance-2-0-260128`      | 720p       | 5 с          | \$0.9074        | ¥6.35  | ¥5.77               | ¥5.29                         | ¥4.97                    |
| `doubao-seedance-2-0-260128`      | 480p       | 5 с          | \$0.4193        | ¥2.94  | ¥2.67               | ¥2.45                         | ¥2.31                    |
| `doubao-seedance-2-0-260128`      | 1080p      | 5 с          | \$2.0288        | ¥14.20 | ¥12.91              | ¥11.84                        | ¥12.39                   |
| `doubao-seedance-2-0-fast-260128` | 720p       | 4 с          | \$0.5814        | ¥4.07  | ¥3.70               | ¥3.39                         | ¥3.20                    |
| `doubao-seedance-2-0-fast-260128` | 720p       | 8 с          | \$1.1568        | ¥8.10  | ¥7.36               | ¥6.75                         | ¥6.40                    |
| `doubao-seedance-2-0-mini-260615` | 720p       | 5 с          | \$0.4508        | ¥3.16  | ¥2.87               | ¥2.63                         | ¥2.50                    |
| `doubao-seedance-2-0-mini-260615` | 480p       | 4 с          | \$0.1681        | ¥1.18  | ¥1.07               | ¥0.98                         | ¥0.93                    |
| `doubao-seedance-2-0-mini-260615` | 720p       | 15 с         | \$1.3451        | ¥9.42  | ¥8.56               | ¥7.85                         | ¥7.47                    |

**Измерения на платформе (с входным видео)** (тестирование проведено 2026-09-12, стандартная модель 1080p / 9:16 / без аудио / референсное видео 12 с; CNY по фиксированному курсу 1:7):

| Модель                       | Разрешение | Выход | Входное видео | Tokens    | Стоимость APIYI | CNY    | Общая цена ÷1.1 (¥) | Для крупного клиента ÷1.2 (¥) | Официальный ориентир (¥) |
| ---------------------------- | ---------- | ----- | ------------- | --------- | --------------- | ------ | ------------------- | ----------------------------- | ------------------------ |
| `doubao-seedance-2-0-260128` | 1080p      | 12 с  | 12 с          | 1,168,425 | \$6.5198        | ¥45.64 | ¥41.49              | ¥38.03                        | ≈ ¥36.2                  |

Входное видео тарифицируется по **отдельной, более низкой ставке tokens** (в этом запуске 1080p измеренная стоимость составила \$5.58 за миллион tokens против \$8.28 для 1080p без видео), однако каждая секунда входного видео преобразуется в tokens с выходным разрешением и тарифицируется вместе с выходом, поэтому общая стоимость обычно выше. **Сначала обрежьте референсное видео**, прежде чем снижать разрешение, если хотите сэкономить.

<Warning>
  **У трёх моделей НЕ одинаковая цена — никогда не считайте их равными.** При одинаковых разрешении и длительности цена за token возрастает в порядке mini \< fast \< standard (например, 720p/5 с: mini ≈ ¥3.16, fast ≈ ¥5.08, standard ≈ ¥6.35), что соответствует официальной ценовой шкале. Для пакетного производства mini **экономит больше всего средств и времени** (в наших тестах 2026-07 её эффективная цена за единицу в точности совпала с номинальной ставкой платформы: отклонение 0.00%); разрешение 1080p доступно только для standard.
</Warning>

Примечание: «CNY» — списание по прайс-листу платформы; «Общая цена ÷1.1» и «Для крупного клиента ÷1.2» — эффективные цены после [бонуса за пополнение](/ru/faq/recharge-promotions) в размере 10% / 20%; после применения бонуса клиенты с крупным пополнением платят примерно 1.05× от официальной ориентировочной цены, а обычные клиенты — около 1.15×. Авторитетным источником данных об использовании является возвращаемый `usage.completion_tokens`.

### Цены Seedance 2.5 (`SeeDance2` группа, 0.18x)

Версии 2.5 и семейство 2.0 используют **одну и ту же `SeeDance2` группу и одинаковый коэффициент 0.18x** — разница между поколениями полностью обусловлена собственными ценами моделей за единицу. Формат 720p/5 с стоит \$1.3721 для 2.5 против \$0.9074 для стандартной модели 2.0 — примерно **в 1,5 раза дороже**. Эта разница **соответствует собственным прайс-листам Volcengine** (тариф Volcengine за token для 2.5 примерно на 52% выше, чем для 2.0); это не наценка APIYI. Оправданность обновления зависит от того, нужны ли вам клипы длительностью 30 секунд, 30 референсных изображений, вывод в формате mov или редактирование/продление видео — если нет, стандартная модель 2.0 дешевле и **также поддерживает 1080p**.

**① Без видео во входных данных** (текст-в-видео, изображение-в-видео, референсные изображения):

| Разрешение | Длительность | Tokens  | Стоимость APIYI | CNY    | Обычные клиенты ÷1.1 (CNY) | Крупные клиенты ÷1.2 (CNY) |
| ---------- | ------------ | ------- | --------------- | ------ | -------------------------- | -------------------------- |
| 480p       | 4 с          | 38,830  | \$0.4893        | ¥3.42  | ¥3.11                      | ¥2.85                      |
| 480p       | 5 с          | 48,437  | \$0.6103        | ¥4.27  | ¥3.88                      | ¥3.56                      |
| 720p       | 5 с          | 108,900 | \$1.3721        | ¥9.60  | ¥8.73                      | ¥8.00                      |
| 720p       | 10 с         | 216,900 | \$2.7329        | ¥19.13 | ¥17.39                     | ¥15.94                     |
| 720p       | 30 с         | 648,900 | \$8.1761        | ¥57.23 | ¥52.03                     | ¥47.69                     |
| 1080p      | 5 с          | 245,025 | \$3.0873        | ¥21.61 | ¥19.65                     | ¥18.01                     |

**② С видео во входных данных** (мультимодальный референс с `video_url`, редактирование видео, продление видео): тарификация выполняется по **отдельной, более низкой ставке за token**.

| Разрешение | Длительность вывода | Входное видео | Tokens | Стоимость APIYI | CNY   |
| ---------- | ------------------- | ------------- | ------ | --------------- | ----- |
| 480p       | 4 с                 | около 5 с     | 86,867 | \$0.6567        | ¥4.60 |

Более низкая ставка не означает меньшую сумму: при наличии видео во входных данных **тарифицируемые tokens = (длительность входного видео + длительность вывода) × площадь**, поэтому само количество token увеличивается. Строка выше стоила бы \$1.0945 по ставке ①; по ставке ② она стоит \$0.6567.

**Две ставки за token**:

| Уровень                                                           | \$ за миллион tokens |
| ----------------------------------------------------------------- | -------------------- |
| ① Без видео во входных данных (одинаково для 480p / 720p / 1080p) | **\$12.60**          |
| ② С видео во входных данных                                       | **\$7.56**           |

И количество token, и обе ставки взяты из фактических журналов тарификации (повторно проверены 2026-08-31), а не рассчитаны по формуле. После применения [бонуса за пополнение](/ru/faq/recharge-promotions) клиенты с пополнением на высоком уровне получают цену, близкую к официальной справочной.

<Warning>
  **Три самых простых способа переплатить за 2.5**:

  1. **`duration` по умолчанию равно `-1`** (для семейства 2.0 по умолчанию используется значение 5). Если не указать этот параметр, 2.5 выберет собственную длительность от 4 до 30 секунд — тестовый запрос без длительности вернул клип длительностью 10 секунд, то есть ровно вдвое дороже варианта на 5 секунд. **Явно указывайте `duration`, если стоимость имеет значение.**
  2. **30 секунд стоят в 6 раз дороже 5 секунд** (¥57.23 против ¥9.60 для 720p). Стоимость строго масштабируется пропорционально длительности, поэтому проверяйте промпт на 5 секундах, прежде чем создавать длинные клипы.
  3. **Длительность референсного видео также тарифицируется.** При использовании `video_url` каждая секунда входного видео преобразуется в tokens с разрешением вывода и тарифицируется вместе с выводом — как в 2.0, так и в 2.5. Предварительное списание учитывает только длительность вывода, поэтому итоговая сумма для таких задач **часто в несколько раз превышает предварительное списание**; это ожидаемо.
</Warning>

Приведённые выше таблицы и расположенная выше таблица для 2.0 **обе используют `SeeDance2` группу с коэффициентом 0.18x**, поэтому их можно напрямую сравнивать. Для `fast` и `mini` также доступны **группы со скидками на ограниченный срок** и более низкими ценами — см. следующий раздел.

<Info>
  **Примечания по тарификации**:

  * Итоговые списания соответствуют ценам моделей в консоли и журналам вызовов
  * **Задачи предварительно тарифицируются при отправке и окончательно рассчитываются после завершения** — ваш баланс на короткое время изменяется; сверяйте данные с журналами вызовов, где одно видео создаёт **две записи о списании** (см. раздел «Чтение списаний в журналах» ниже)
  * **Резервирование зависит только от длительности и не зависит от разрешения**: \$0.09/секунду для семейства 2.0 и \$0.135/секунду для 2.5. Поэтому для 1080p обычно выполняется дополнительное списание, а для 480p обычно возвращается часть средств — оба варианта нормальны
  * Отклонённые запросы (ошибки параметров HTTP 400 и т. п.) **не тарифицируются** (проверено)
  * Стоимость масштабируется линейно относительно длительности: видео длительностью 15 с стоит примерно в 3 раза дороже видео длительностью 5 с
</Info>

### Группы с ограниченной по времени скидкой (только mini / fast, до 10/7)

<Info>
  **8 августа 2026 года запущены две группы с ограниченной по времени скидкой**: `SD2Mini` (коэффициент **0.10x**) и `SD2Fast` (коэффициент **0.15x**). По сравнению с обычной группой `SeeDance2` с коэффициентом 0.18x это **скидка 44.4% для mini и 16.7% для fast**. Возможности моделей, параметры, эндпоинты и синтаксис вызова не изменились — **замените один Token и не меняйте код**. Предложение действует до **2026-10-07 23:59 (UTC+8)** (продлено 2026-09-05 одновременно с официальной акцией Volcengine; первоначальной датой окончания было 7 сентября).
</Info>

**Сопоставимое сравнение цен** (рассчитано на основе указанных выше измеренных значений пропорционально коэффициенту новой группы, то есть делением на 0.18 и умножением на новый коэффициент; CNY конвертированы по фиксированному курсу 1:7, только для справки):

| Модель | Разрешение / длительность | Обычная `SeeDance2` (0.18x) | Группа со скидкой | Стоимость со скидкой | Снижение |
| ------ | ------------------------- | --------------------------- | ----------------- | -------------------- | -------- |
| `mini` | 720p / 5s                 | \$0.4508 (¥3.16)            | `SD2Mini`         | **\$0.2504 (¥1.75)** | −44.4%   |
| `mini` | 480p / 4s                 | \$0.1681 (¥1.18)            | `SD2Mini`         | **\$0.0934 (¥0.65)** | −44.4%   |
| `mini` | 720p / 15s                | \$1.3451 (¥9.42)            | `SD2Mini`         | **\$0.7473 (¥5.23)** | −44.4%   |
| `fast` | 720p / 5s                 | \$0.7253 (¥5.08)            | `SD2Fast`         | **\$0.6044 (¥4.23)** | −16.7%   |
| `fast` | 480p / 5s                 | \$0.3373 (¥2.36)            | `SD2Fast`         | **\$0.2811 (¥1.97)** | −16.7%   |
| `fast` | 720p / 4s                 | \$0.5814 (¥4.07)            | `SD2Fast`         | **\$0.4845 (¥3.39)** | −16.7%   |
| `fast` | 720p / 8s                 | \$1.1568 (¥8.10)            | `SD2Fast`         | **\$0.9640 (¥6.75)** | −16.7%   |

Та же модель, те же характеристики: группы со скидкой позволяют сэкономить **44.4% на mini и 16.7% на fast**. Это прямое снижение коэффициента группы, которое независимо суммируется с [бонусом за пополнение](/ru/faq/recharge-promotions). Тарификация не изменилась — расчёт по-прежнему выполняется по фактическому использованию token, при этом отклонение ±5% является нормальным.

<Warning>
  **После окончания предложения ничего не отключается**: после 2026-10-07 23:59 (UTC+8) `SD2Mini` и `SD2Fast` **останутся доступными** — их коэффициент просто вернётся к 0.18x, как у обычной группы `SeeDance2`. Ваши Tokens продолжат работать, и изменения кода не потребуются. Если вы планируете пакетное производство, запланируйте его в период действия скидки.
</Warning>

### Просмотр списаний в логах (предварительное списание + окончательный расчёт)

Откройте страницу логов консоли по адресу `api.apiyi.com/log` и найдите название модели `doubao-seedance-2-0`, чтобы увидеть все списания. **Одно видео создаёт две записи о списании**:

1. **Предварительное списание**: расчётная сумма, удерживаемая при отправке задачи (запись в логе с пометкой «non-streaming», содержащая token и группу) — \$0.449998 на снимке экрана ниже
2. **Окончательный расчёт (списание или возврат)**: после завершения задачи разница рассчитывается на основе фактически сгенерированных token (запись в логе с пометкой «streaming» и количеством completion-token) — ниже указано \$5.611858; **для 1080p обычно взимается дополнительная плата**

<Frame caption="Two charge entries for one 15 s 1080p video: pre-charge + settlement">
  <img src="https://mintcdn.com/apiyillc/ae60mWKk0AtXJTS1/images/seedance2-billing-log-two-entries.png?fit=max&auto=format&n=ae60mWKk0AtXJTS1&q=85&s=9e6583b5467c886a02da82513eb6a154" alt="Страница логов APIYI с двумя записями о списании за одно видео Seedance 2.0: предварительное списание и окончательный расчёт" width="2000" height="624" data-path="images/seedance2-billing-log-two-entries.png" />
</Frame>

<Note>
  В записи об окончательном расчёте **нет ни token, ни его группы** — это нормально. Сумма двух записей составляет общую стоимость видео.
</Note>

**Как читать поля времени**:

1. Временная метка первой записи (предварительного списания) — это **время отправки** видео; значение «first byte» показывает, сколько времени заняла отправка до получения ID задачи (например, `首字节:3秒` / first byte: 3 с), — **а не время генерации**
2. В записи об окончательном расчёте указаны `流式` (streaming) и `首字节:<1秒` (first byte менее 1 с) — это всего лишь внутренние маркеры записи об окончательном расчёте, **а не признак какой-либо проблемы**
3. Фактическое **время генерации** видео указано в столбце «耗时» (истекшее время) на странице «Асинхронные задачи» (`api.apiyi.com/task`) в верхней навигации

<Frame caption="The first log entry's timestamp = submission time, and its first-byte value (3 s) is the submission latency; this fast example settled as a refund (negative amount), total cost 0.360000 − 0.022750 = 0.337250 USD">
  <img src="https://mintcdn.com/apiyillc/ae60mWKk0AtXJTS1/images/seedance2-billing-log-time-fields.png?fit=max&auto=format&n=ae60mWKk0AtXJTS1&q=85&s=ec4fd88847492864468cc91ffb643ca9" alt="Просмотр полей времени и first byte на странице логов: первая запись содержит время отправки и задержку отправки" width="1248" height="332" data-path="images/seedance2-billing-log-time-fields.png" />
</Frame>

<Frame caption="The elapsed column on the Async tasks page is the actual video generation time, e.g. 158 s, 303 s">
  <img src="https://mintcdn.com/apiyillc/ae60mWKk0AtXJTS1/images/seedance2-task-page-elapsed-time.png?fit=max&auto=format&n=ae60mWKk0AtXJTS1&q=85&s=9c1b2073b12afebcf1182246a6685a71" alt="Страница «Асинхронные задачи», на которой для каждой задачи видео указаны время отправки и истекшее время генерации" width="1506" height="532" data-path="images/seedance2-task-page-elapsed-time.png" />
</Frame>

Для видео длительностью 15 с в разрешении 1080p на первом снимке экрана общая стоимость = 0.449998 + 5.611858 = **\$6.061856**. Соответствующие параметры задачи отображаются в разделе «Асинхронные задачи» в верхней части `api.apiyi.com/task` и полностью совпадают со списаниями:

```json theme={null}
{
  "id": "cgt-20260703185641-9nbbg",
  "model": "doubao-seedance-2-0-260128",
  "status": "succeeded",
  "duration": 15,
  "resolution": "1080p",
  "ratio": "3:4",
  "framespersecond": 24,
  "generate_audio": true
}
```

732 108 completion-token ≈ 15 × 1248 × 1664 × 24 / 1024 (видео 3:4 в разрешении 1080p создаётся с размером 1248×1664) — это соответствует формуле тарификации.

<Note>
  Это видео длительностью 15 с в разрешении 1080p стоит в общей сложности около **¥42.4** (номинальная плата при фиксированном курсе 1:7); с учётом [бонуса за пополнение](/ru/faq/recharge-promotions) фактическая стоимость составляет примерно ¥35–39 по сравнению с официальной ориентировочной ценой около ¥37.2 за те же характеристики. **Сама официальная цена невысокой не является** — стоимость определяется сочетанием **модели + разрешения + длительности** (переключение на fast / 720p / 5 с обходится значительно дешевле). Эта модель предоставляется с небольшой маржой для обеспечения доступности, а клиенты с крупными депозитами получают большие скидки.
</Note>

<Note>
  **Уведомление о бета-поставке**: Seedance 2.5 и семейство 2.0 в настоящее время находятся на этапе бета-поставки. Если ваши фактические списания заметно отличаются от приведённой выше таблицы, обратитесь в службу поддержки, и мы выполним сверку. Тарификация будет динамически корректироваться в соответствии с политикой вышестоящего поставщика (например, если позднее появится официальный вариант с более низкой ценой) и возможностями поставок APIYI; надёжные партнёры каналов продаж могут связаться с нами. Эта модель тарифицируется для **обеспечения поставок и обслуживания клиентов**, а не для получения прибыли.
</Note>

### Поиск стоимости одного видео по ID задачи: используйте task API

Для программной сверки **не пытайтесь сопоставлять записи логов по одной**: ни одна из двух записей лога видео не содержит `task_id`, а запись расчёта имеет пустое `request_id` (так это работает на документированном пути `/seedance/api/v3/...`). Используйте **task API** и выполняйте запрос напрямую по `task_id`, аутентифицируясь тем же системным token, что и для [Log Query API](/ru/api-capabilities/log-query):

```bash theme={null}
curl --compressed -s "https://api.apiyi.com/api/task/self?p=1&page_size=1&task_id=cgt-20260915152401-grlz4" \
  -H "Authorization: $APIYI_SYS_TOKEN" | jq '.data.items[0] | {task_id, status, quota, submit_time, finish_time, model_name}'
```

Возвращаемое значение **`quota` — это общая стоимость видео** (предварительное списание + расчёт, ÷ 500 000 = USD). Это то же число, которое вы получите, сложив две записи логов, и то же `quota`, отображаемое в деталях задачи на странице «Асинхронные задачи». Интерпретируйте его по `status`:

| `status`                    | Что означает `quota`                                                    | Фактическая стоимость видео                                                                                                |
| --------------------------- | ----------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| `completed`                 | Итоговая сумма после расчёта                                            | = `quota`                                                                                                                  |
| `submitted` / `in_progress` | Только предварительное списание, выполненное при отправке               | Дождитесь завершения                                                                                                       |
| `failed`                    | **По-прежнему отображает предварительное списание** (оно не обнуляется) | **0**: предварительное списание полностью возвращается, с соответствующей отрицательной записью возврата `type=11` в логах |

Обратите внимание, что используются разные словари: эндпоинт запроса видео `/seedance/api/v3/.../tasks/{id}` сообщает об успехе как `succeeded`, тогда как task API `/api/task/self` сообщает о нём как `completed`.

<Warning>
  **Параметры пагинации task API противоположны параметрам Log Query API**: здесь используется snake\_case `page_size`, а номер страницы `p` начинается с **1**; log API использует camelCase `pageSize`, где `p` начинается с 0. Ошибка не вызывает ошибку, вы просто получите страницу по умолчанию. Проверенные фильтры: `task_id`, `model_name`, `start_timestamp` / `end_timestamp` (Unix seconds).
</Warning>

Если необходимо выполнить ручную перекрёстную проверку в Log Query API: timestamp записи расчёта совпадает с `finish_time` задачи (с точностью до 1 секунды), её `completion_tokens` равен `usage.completion_tokens` задачи, `other.final_quota` равен `quota` задачи, а `other.original_quota` — это предварительное списание. Когда несколько задач отправляются в одну и ту же секунду, их записи предварительного списания совпадают, поэтому это подходит только для ручных проверок, но не для автоматического сопоставления. Полное руководство: [Как найти фактическую стоимость видео по task\_id](/ru/faq/seedance-task-cost-lookup).

## Настройка групп

Seedance 2.5 и семейство 2.0 работают в **выделенной группе**, при соблюдении двух **обязательных условий**: ① модель тарификации Token должна быть **Pay-as-you-go Priority** (или Pay-as-you-go) — Tokens с оплатой за запрос не могут быть направлены; ② для Token должна быть включена **соответствующая группа**. Tokens в группе Default или других группах видео завершатся ошибкой "**no available channel for this model**".

Сейчас доступно три группы. **2.5 и семейство 2.0 используют общую `SeeDance2`**, а также есть две группы с ограниченной по времени скидкой, каждая из которых обслуживает ровно одну модель:

| Группа      | Коэффициент тарифа | Обслуживаемые модели                                 | Примечания                                                                                                                        |
| ----------- | ------------------ | ---------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| `SeeDance2` | 0.18x              | **Все четыре модели** (2.5 / standard / fast / mini) | Единственный маршрут для 2.5 и стандартной модели; резервный вариант после окончания акции — высокая параллельность, без очередей |
| `SD2Mini`   | **0.10x**          | Только `doubao-seedance-2-0-mini-260615`             | 🔥 Ограниченное по времени предложение, **на 44.4% ниже** исходного коэффициента, до 2026-10-07 23:59 (UTC+8)                     |
| `SD2Fast`   | **0.15x**          | Только `doubao-seedance-2-0-fast-260128`             | 🔥 Ограниченное по времени предложение, **на 16.7% ниже** исходного коэффициента, до 2026-10-07 23:59 (UTC+8)                     |

<Note>
  **Один Token `SeeDance2` открывает доступ ко всем четырём моделям**: 2.5 и три модели семейства 2.0 находятся в этой группе, поэтому в вашем коде меняется только поле `model`.

  **Две скидочные группы являются каналами для одной модели**: `SD2Mini` обслуживает только mini, а `SD2Fast` — только fast, поэтому вызов любой другой модели через них вернёт ту же ошибку.

  **После окончания акции отключения не будет**: после 2026-10-07 23:59 (UTC+8) обе скидочные группы останутся онлайн, а коэффициент тарифа вернётся к 0.18x — изменения Token или кода не требуются.
</Note>

<Note>
  **Для 2.5 используется тот же коэффициент 0.18x.** Разница между поколениями обусловлена собственными ценами моделей за единицу (цены Volcengine для 2.5 выше, чем для 2.0), а не коэффициентом группы — поэтому 2.5 и семейство 2.0 работают по совершенно одинаковой базе конвертации.

  **Почему 0.18x?** Встроенные в систему цены за единицу для Seedance соответствуют официальным прайс-листам Volcengine — но этот прайс-лист указан в **CNY**, тогда как балансы APIYI указаны в **USD** (фиксированный курс USD/CNY 1:7). При коэффициенте 1x фактически взималось бы в 7 раз больше официальной суммы, поэтому коэффициент группы снижен для компенсации конвертации валюты — **именно отсюда берётся 0.18: это не скидка и не наценка**.

  Volcengine **не предоставляет скидок** на эту серию, а цена этого канала установлена для обеспечения поставок. По умолчанию номинальное списание немного выше официальной ориентировочной цены, но [бонус пополнения](/ru/faq/recharge-promotions) в значительной мере это компенсирует: **клиенты с крупными пополнениями платят всего примерно на 5% больше**, а некоторые уровни (например, 1080p) оказываются ниже официальной цены.

  **Обратите внимание**: тарификация всегда основана на **фактическом использовании token**, а конвертация token имеет небольшое естественное отклонение (±5% — это нормально); официальный прайс-лист является лишь **ориентиром**, а не гарантией для каждого запроса. Текущая цена — это разумная схема с приоритетом обеспечения поставок, поэтому всегда оценивайте её **вместе с бонусом пополнения**. Если списание выглядит некорректным, мы в любое время готовы сверить с вами начисления; однако вопрос «почему цена немного выше официальной» не подлежит обсуждению — пожалуйста, учитывайте это и не используйте данный канал, если это вызывает сомнения. В свою очередь, именно этот канал обеспечивает **высокую параллельность без очередей**.

  Кроме того, [библиотека ассетов](/ru/api-capabilities/seedance2/asset-library) (загрузка виртуальных аватаров / верификация реальных людей) **бесплатно включена в этот канал** — официально для её отдельной покупки требуется годовой контракт на шестизначную сумму в CNY. Это является частью реальной ценности данного канала.
</Note>

### Как настроить ваши Tokens

**Если вам не нужны скидки**: создайте один Token с включённой группой `SeeDance2` — он обеспечивает доступ **ко всем четырём моделям** — и пропустите таблицу ниже.

**Если вам нужны ограниченные по времени скидки**: у `mini` и `fast` есть собственные группы для одной модели, поэтому разделите Tokens следующим образом:

| Token                           | Основная группа | Что запускается                                                               |
| ------------------------------- | --------------- | ----------------------------------------------------------------------------- |
| **A (новый · скидка)**          | `SD2Mini`       | Только `doubao-seedance-2-0-mini-260615`, по коэффициенту 0.10x               |
| **B (новый · скидка)**          | `SD2Fast`       | Только `doubao-seedance-2-0-fast-260128`, по коэффициенту 0.15x               |
| **C (существующий · основной)** | `SeeDance2`     | 2.5 и стандартная модель 2.0; всё семейство 2.0 перейдёт сюда после 7 октября |

Каждый Token должен использовать модель тарификации Pay-as-you-go Priority (или Pay-as-you-go). Если вы используете только mini, достаточно одного Token A; если только 2.5 — достаточно одного Token C — все они вам не нужны.

<Tip>
  **Почему отдельный скидочный Token оправдан**:

  * **Вы не пропустите срок** — тарификация разделена по Token, поэтому сразу видно, что вы использовали и сколько сэкономили во время акции, а также легче решить, стоит ли перенести пакетные задачи на более ранний срок по мере приближения 7 октября
  * **Переключение ничего не стоит** — когда акция закончится, направьте клиент обратно на Token C; изменения кода или групп не нужны
  * **Выделенные Tokens в любом случае рекомендуются для production** — контроль квот и оповещения для каждой бизнес-линии, а также значительно более простое отслеживание неожиданного скачка потребления
</Tip>

<Info>
  **Seedance 2.5 уже доступен** (2026-08-28): имя модели — `doubao-seedance-2-5-260628`, она находится в той же **группе `SeeDance2`** (0.18x), что и семейство 2.0. Эндпоинт, аутентификация и формат запросов идентичны 2.0 — **замените поле `model`, и ваш код продолжит работать**. По сравнению с семейством 2.0: ограничение длительности 15 с → **30 с**, референсные изображения 9 → **30**, референсные видео/аудио 3 → **10**, аудио можно использовать отдельно, а также доступны вывод **mov** и селектор задач `omni_reference_task_type`. Модель работает примерно в 1.5× быстрее стандартной модели 2.0. Полное сравнение смотрите ниже в разделе «Технические характеристики».
</Info>

## Технические характеристики

| Параметр                         | Seedance 2.5                                                                                                    | Семейство Seedance 2.0 (standard / fast / mini)                                                |
| -------------------------------- | --------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------- |
| **Модели**                       | `doubao-seedance-2-5-260628`                                                                                    | `doubao-seedance-2-0-260128` / `-fast-260128` / `-mini-260615`                                 |
| **Разрешения**                   | 480p / 720p / **1080p** (без 4k)                                                                                | 480p / 720p / 1080p (1080p только для standard; fast и mini ограничены 720p)                   |
| **Видеокодек**                   | Выходы 1080p: **H.265 (hvc1)**; выходы 480p / 720p: H.264 (avc1)                                                | H.264 (avc1)                                                                                   |
| **Соотношения сторон**           | Как справа; **первый/последний кадр, редактирование и расширение принудительно устанавливают `adaptive`**       | `16:9` `4:3` `1:1` `3:4` `9:16` `21:9` `adaptive` (по умолчанию адаптивное)                    |
| **Длительность**                 | **4–30** целых секунд или выбираемая моделью `-1` (**по умолчанию — `-1`**)                                     | 4–15 целых секунд или выбираемая моделью `-1` (по умолчанию 5)                                 |
| **Частота кадров**               | Фиксированная 24 fps (параметр `frames` не поддерживается)                                                      | То же                                                                                          |
| **Формат вывода**                | `output_format`: `mp4` (по умолчанию) или **`mov`** (H.264 + yuv444p + PCM, для профессиональной постобработки) | Только mp4                                                                                     |
| **Аудио**                        | `generate_audio` по умолчанию имеет значение `true`; моно                                                       | То же                                                                                          |
| **Ограничения ссылок**           | **30 изображений + 10 видео + 10 аудиоклипов**; **аудио может быть единственной ссылкой**                       | 9 изображений + 3 видео + 3 аудиоклипа; аудио должно сопровождать изображение или видео        |
| **Параметр типа задачи**         | `omni_reference_task_type`: `auto` / `edit` (редактирование видео) / `extend` (расширение видео)                | Не поддерживается                                                                              |
| **Входные изображения**          | jpeg/png/webp/bmp/tiff/gif/heic/heif; соотношение сторон (0.4, 2.5); стороны (300, 6000) px; менее 30 MB каждое | То же                                                                                          |
| **Время генерации (измеренное)** | 480p/4s: \~1.5-5 мин; 720p/5s: \~2.5 мин; **720p/30s: \~5.5 мин**; 1080p/5s: \~2.5 мин                          | 5 s @720p: \~2-5 мин; 1080p: \~3 мин; 15 s: \~4.5 мин; mini быстрее (5 s @720p: \~1.5-2.5 мин) |
| **Поля ответа**                  | `content.video_url` (прямая ссылка, **истекает через 24 ч**), `usage.completion_tokens`                         | То же                                                                                          |
| **Хранение задач**               | task\_id можно запрашивать в течение 7 дней                                                                     | То же                                                                                          |

## Эндпоинты API

| Эндпоинт                                               | Назначение                                    | Content-Type       |
| ------------------------------------------------------ | --------------------------------------------- | ------------------ |
| `POST /seedance/api/v3/contents/generations/tasks`     | Создание задачи генерации видео               | `application/json` |
| `GET /seedance/api/v3/contents/generations/tasks/{id}` | Проверка статуса задачи / получение URL видео | —                  |

<Tip>
  **Домены**: `api.apiyi.com` — основной шлюз; `vip.apiyi.com` и другие домены платформы работают идентично. Префикс пути — `/seedance/api/v3` — **не удаляйте сегмент `/api`**.
</Tip>

<Warning>
  **Не отправляйте задачи Seedance через универсальные эндпоинты видео.** `/v1/videos`, `/v1/video/generations` и `/v2/videos/generations` в настоящее время не передают параметр Seedance `resolution` полностью — в рабочей среде мы измерили **2,5 запроса для рендеринга в разрешении 480p при 720p и 2,0 запроса семейства для рендеринга в разрешении 1080p при 720p**, при этом тарификация выполнялась по фактически отрендеренным token (в первом случае — на 2,25× больше). Проблема выявлена и исправляется (2026-09-14); до этого используйте только эндпоинты `/seedance/api/v3/...` на этой странице, где разрешение учитывается. Если с вас списали средства за несоответствующее разрешение через универсальный эндпоинт, обратитесь в службу поддержки для возврата разницы на основании запрошенного разрешения.
</Warning>

## Разрешения и соотношения сторон в деталях

Уровень разрешения определяет **площадь в пикселях**, а не короткую сторону. Фактические размеры вывода для каждого соотношения (официальные значения, проверенные в наших тестах):

| Соотношение | 480p                                                                     | 720p     | 1080p (только 2.5 / стандартное) |
| ----------- | ------------------------------------------------------------------------ | -------- | -------------------------------- |
| `16:9`      | 864×496                                                                  | 1280×720 | 1920×1080                        |
| `4:3`       | 752×560                                                                  | 1112×834 | 1664×1248                        |
| `1:1`       | 640×640                                                                  | 960×960  | 1440×1440                        |
| `3:4`       | 560×752                                                                  | 834×1112 | 1248×1664                        |
| `9:16`      | 496×864                                                                  | 720×1280 | 1080×1920                        |
| `21:9`      | 992×432                                                                  | 1470×630 | 2206×946                         |
| `adaptive`  | Модель выбирает одно из указанных выше значений на основе входных данных | То же    | То же                            |

<Note>
  **В 2.5 разрешение 480p с соотношением 16:9 составляет 854×480** (измерено), а не 864×496, которое выдаёт 2.0, — площадь немного меньше, поэтому одна и та же спецификация стоит немного меньше token. Все остальные измеренные нами уровни совпадают в обоих поколениях: 480p 1:1 = 640×640, 720p 16:9 = 1280×720, 720p 21:9 = 1470×630, 1080p 16:9 = 1920×1080.

  **Ни одна модель этого семейства не поддерживает `4k`** — отправка `"resolution": "4k"` возвращает синхронную ошибку 400 (не тарифицируется).
</Note>

### Как работает адаптивный режим

1. **Текст-видео**: модель определяет оптимальное соотношение на основе вашего prompt
2. **Первый+последний / первый кадр**: используется соотношение изображения первого кадра (изображения с несовпадающим соотношением обрезаются по центру)
3. **Мультимодальное преобразование по референсу в видео**: учитывается намерение, заданное в prompt; в противном случае используется первый медиаэлемент (видео имеет приоритет над изображениями)
4. **Редактирование / расширение видео (2.5)**: соотношение вывода соответствует входному видео, которое редактируется или расширяется
5. Фактически использованное соотношение возвращается в поле `ratio` ответа задачи

<Warning>
  `ratio` принимает только 7 указанных выше значений перечисления — передача, например, `"2:1"` возвращает ошибку `InvalidParameter` (проверено); то же происходит при передаче `duration` за пределами поддерживаемого диапазона (4–30 в 2.5, 4–15 в семействе 2.0). Ни один из этих вариантов не тарифицируется.

  **В 2.5 дополнительно требуется `ratio: adaptive` для трёх типов задач**: генерация по первому кадру / по первому и последнему кадрам, редактирование видео и расширение видео. Передача конкретного соотношения сторон в этих случаях возвращает `InvalidParameter.TaskTypeConstraint` **на этапе отправки** (измерено: синхронная ошибка 400, а не задача, завершающаяся ошибкой через несколько минут).
</Warning>

## Лучшие практики

<Steps>
  <Step title="Выбирайте модель в соответствии с требованиями к результату">
    **Сначала выясните, нужно ли вам то, что есть только в 2.5**: ролики длительностью 30 секунд, 30 эталонных изображений, отдельные эталонные аудиозаписи, вывод в формате mov или явные типы задач редактирования/продления видео. Если нужно что-либо из этого, выберите `doubao-seedance-2-5-260628` (примерно в 1,5 раза дороже стандартной модели, в той же группе, что и семейство 2.0). Если нет, используйте семейство 2.0: **для пакетного производства и задач, чувствительных к стоимости, используйте lite-модель** `doubao-seedance-2-0-mini-260615` (примерно вдвое дешевле стандартной модели и обеспечивает самую быструю генерацию, с ограничением до 720p); для 1080p или максимального качества используйте стандартную модель; `fast` — промежуточный вариант.
  </Step>

  <Step title="Сначала загружайте изображения и видео, чтобы получить ID актива">
    Когда запрос содержит медиафайлы, задержка создания задачи в основном определяется **загрузкой этих медиафайлов**: встроенный Base64 или URL большого изображения увеличивает время отправки примерно с одной секунды до десятков секунд либо приводит к тайм-ауту чтения. Сначала загрузите медиафайл, получите ID актива `asset://` и укажите его в запросе — размер тела запроса сократится до нескольких десятков байт, ID задачи вернётся немедленно, а проверки содержимого будут выполнены на этапе загрузки. См. [Рабочий процесс с предварительной загрузкой актива](/ru/api-capabilities/seedance2/asset-first-workflow).
  </Step>

  <Step title="Используйте adaptive, чтобы избежать обрезки">
    Для преобразования изображения в видео сохраняйте значение по умолчанию `adaptive`, чтобы модель сопоставляла соотношение сторон исходного изображения. Фиксируйте `9:16` (книжная ориентация) или `16:9` (альбомная ориентация) только при наличии соответствующего требования целевой платформы.
  </Step>

  <Step title="Длительность — ваш регулятор стоимости">
    Стоимость строго пропорциональна длительности. Сначала проверяйте промпты на роликах длительностью 5 с, затем увеличивайте длительность (2.5 поддерживает ролики до 30 с, что в 6 раз дороже ролика длительностью 5 с). **В 2.5 всегда явно передавайте `duration`** — значение по умолчанию равно `-1`, поэтому при его отсутствии модель выбирает значение самостоятельно, а при тестировании выбиралось 10 секунд, что удваивало стоимость.
  </Step>

  <Step title="Отключайте аудио, если оно вам не нужно">
    `generate_audio` по умолчанию имеет значение true. Передавайте `false` для видео без звука, которое вы планируете озвучить самостоятельно.
  </Step>

  <Step title="Заключайте реплики в кавычки для улучшения закадрового голоса">
    Помещайте произносимые реплики в двойные кавычки в промпте — модель автоматически сгенерирует соответствующие голоса.
  </Step>

  <Step title="Добавляйте Accept-Encoding: identity в HTTP-клиенты">
    Шлюз помечает ответы как `content-encoding: gzip`, хотя тело ответа не сжато; клиенты с автоматической распаковкой, например Python requests, вызывают `ContentDecodingError`. Добавление заголовка `Accept-Encoding: identity` устраняет эту проблему (curl это не затрагивает).
  </Step>

  <Step title="Проверяйте статус каждые 15–30 с и сразу скачивайте результат">
    Задачи обычно выполняются за 2–5 минут. `content.video_url` — это подписанная ссылка, действительная в течение 24 часов; после успешного выполнения задачи сразу скопируйте файл в собственное хранилище.
  </Step>

  <Step title="Объединяйте ролики с помощью return_last_frame">
    Установите `return_last_frame: true`, чтобы получить PNG последнего кадра без водяного знака, а затем используйте его в качестве первого кадра следующей задачи для создания непрерывных видео из нескольких роликов.
  </Step>
</Steps>

## Коды ошибок и повторные попытки

| Код                                                   | Значение                                                                                                                                               | Рекомендуемая обработка                                                                                                                                                                            |
| ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `400`                                                 | `InvalidParameter`: некорректное разрешение/соотношение сторон/длительность (например, fast или mini + 1080p, любая модель + 4k, 2.5 + `duration: 31`) | В сообщении указан некорректный параметр — исправьте его согласно таблицам выше; не тарифицируется                                                                                                 |
| `400`                                                 | `InvalidParameter.TaskTypeConstraint` (**только 2.5**): параметры конфликтуют с типом задачи                                                           | Установите для `ratio` значение `adaptive` для задач с первым/последним кадром, редактированием и расширением; установите для `duration` значение `-1` для задач редактирования; не тарифицируется |
| Задача `failed` + `InvalidParameter.TaskTypeMismatch` | **только 2.5**: указанное `omni_reference_task_type` не соответствует типу задачи, который модель определила по prompt                                 | Приведите prompt в соответствие с нужной задачей (редактирование: добавить / удалить / заменить; расширение: продлить / продолжить) или переключитесь на `auto`                                    |
| `401`                                                 | Недействительный Token                                                                                                                                 | Проверьте Bearer Token                                                                                                                                                                             |
| `403`                                                 | Отклонение модерацией контента (реальные лица, нарушения политики)                                                                                     | Измените ресурсы или prompt                                                                                                                                                                        |
| `429`                                                 | Достигнут лимит запросов / недостаточная квота                                                                                                         | Экспоненциальная задержка; проверьте баланс                                                                                                                                                        |
| `5xx`                                                 | Ошибка шлюза / backend                                                                                                                                 | Повторите попытку 1–2 раза                                                                                                                                                                         |
| Задача `failed`                                       | Генерация не удалась                                                                                                                                   | Проверьте поле ошибки задачи; при необходимости повторите попытку с другим seed                                                                                                                    |
| Задача `expired`                                      | Превышено `execution_expires_after` (по умолчанию 48 ч)                                                                                                | Отправьте повторно                                                                                                                                                                                 |

<Info>
  **Рекомендации для клиента**:

  * Тайм-аутов запросов в 30–60 с достаточно для вызовов создания/опроса (ожидание происходит на стороне задачи)
  * Выполняйте опрос каждые 15–30 с с общим лимитом времени **15+ минут** (дольше для задач 1080p / 15 с)
  * Применяйте **экспоненциальную задержку** при 5xx и тайм-аутах (2 повторные попытки)
  * Для устранения неполадок записывайте `id` задачи и заголовок ответа `X-Shellapi-Request-Id` (ID запроса APIYI, доступный для поиска в логах; `X-Request-Id` — ID на стороне провайдера)
</Info>

## Часто задаваемые вопросы

<AccordionGroup>
  <Accordion title="Почему запрос с изображениями или видео так долго возвращает идентификатор задачи или завершается по тайм-ауту?">
    Медленной является **отправка**, а не генерация. Медиафайл должен пройти через APIYI, затем быть перенаправлен в Volcengine, где он декодируется и проверяется, — идентификатор задачи возвращается только после завершения всех этих операций. При использовании встроенного Base64 или URL большого изображения этот этап занимает не около секунды, а десятки секунд, и даже тайм-аут чтения клиента в 60 секунд может оказаться недостаточным. Сама генерация обычно занимает 2–5 минут — это нормальная скорость провайдера, не связанная со скоростью отправки.

    Реальное решение — **сначала загрузить медиафайл и передать на него ссылку как на `asset://` идентификатор ресурса**, что уменьшает тело запроса с мегабайт до нескольких десятков байт. Разбор задержек, шаги миграции и способ проверить, была ли задача создана после тайм-аута, описаны в разделе [Рабочий процесс сначала с ресурсами](/ru/api-capabilities/seedance2/asset-first-workflow).
  </Accordion>

  <Accordion title="Seedance 2.5 или 2.0 — что выбрать?">
    **Выбирайте с учётом необходимых возможностей — 2.5 не является автоматическим вариантом по умолчанию.** Она работает примерно в 1,5 раза дороже стандартной модели 2.0 (720p/5 с: \$1.3721 против \$0.9074), что соответствует разнице в собственных прейскурантных ценах Volcengine.

    **Выбирайте 2.5, если** нужны ролики **длиннее 15 секунд** (2.5 поддерживает до 30 секунд), **более 9 эталонных изображений** (2.5 принимает 30), формат вывода **mov** или **редактирование / расширение видео** с выявлением ошибок параметров уже на этапе отправки (`omni_reference_task_type`). 2.5 также позволяет использовать **аудио как единственный эталон**, тогда как 2.0 требует изображения или видео в дополнение к нему.

    **Оставайтесь в семействе 2.0, если** ваши ролики короче 15 секунд: стандартная модель **также поддерживает 1080p** и относится к тому же флагманскому уровню качества; для пакетного производства с учётом стоимости используйте `mini` — его цена за единицу примерно вдвое ниже стандартной, а генерация выполняется быстрее всего. Семейство 2.0 не выводится из эксплуатации.

    Эндпоинт, авторизация и структура запроса идентичны для всех поколений, как и **группа** — для переключения достаточно изменить одно поле: `model`.
  </Accordion>

  <Accordion title="Поддерживает ли 2.5 разрешение 1080p? А 4k?">
    **1080p работает** (проверено: видео 1920×1080 создаётся без проблем). **4k не поддерживается** — отправка `"resolution": "4k"` возвращает синхронную ошибку 400 (тарификация не выполняется).

    Есть одно важное отличие, которое легко пропустить: **2.5 кодирует 1080p в H.265 (hvc1)**, тогда как для 480p и 720p используется H.264 (avc1). Файлы H.265 меньше, но старые проигрыватели, некоторые браузеры и отдельные пакеты для монтажа работают с ним менее надёжно, чем с H.264. Перед распространением видео 1080p убедитесь, что ваш последующий конвейер может его декодировать.
  </Accordion>

  <Accordion title="Как выполнять редактирование и расширение видео в 2.5?">
    Оба режима представляют собой задачи с «омни-эталоном»: они запускаются эталонным видео в `content` и намерением, выраженным в промпте. Явно передавайте `omni_reference_task_type`, чтобы ошибки выявлялись на раннем этапе:

    * **Редактирование видео**: `omni_reference_task_type: "edit"`, как минимум один `role: "reference_video"`, **`ratio` должен иметь значение `adaptive`, а `duration` — значение `-1`**, исходное видео должно длиться 4–30 секунд. В промпте должен быть глагол редактирования (добавить, удалить, изменить, заменить). Соотношение сторон и длительность вывода соответствуют входному видео, причём **длительность может быть дробной** (в одном проверенном запуске результат длился 16,709 секунды).
    * **Расширение видео**: `omni_reference_task_type: "extend"`, также с эталонным видео и **`ratio`, установленным в `adaptive`**. В промпте должен быть глагол расширения (продолжить, продлить).

    Ссылайтесь на ресурсы в промпте по их позициям — `@video1`, `@image1` — в том порядке, в котором они были переданы. Недопустимые параметры возвращают **ошибку 400 при отправке** (`InvalidParameter.TaskTypeConstraint`), а не приводят к сбою задачи через несколько минут.
  </Accordion>

  <Accordion title="Для чего в 2.5 нужен формат вывода mov?">
    Передача `"output_format": "mov"` возвращает контейнер QuickTime (H.264 + цветовая субдискретизация yuv444p + аудио PCM) с более высокой точностью передачи цвета и яркости — он подходит для цветокоррекции, кейинга и композитинга и официально рекомендован как для входных, так и для выходных данных при редактировании и расширении видео. По умолчанию используется `mp4`, обеспечивающий наиболее широкую совместимость.

    **Учтите, что mov использует профессиональные кодеки, которые поддерживаются не всеми проигрывателями** (с ними работают VLC, mpv, ffplay и IINA в macOS). Для прямого распространения в интернете или на мобильных устройствах используйте формат mp4 по умолчанию.
  </Accordion>

  <Accordion title="Я получаю сообщение «no available channel for this model» — почему?">
    Это самая распространённая ошибка Seedance: в девяти случаях из десяти для токена включена неправильная группа. В сообщении об ошибке указывается текущая группа, например:

    ```
    Current group SeeDance2 has no available channels
    for model doubao-seedance-2-5-260628
    ```

    Сопоставьте модель с её группой и включите эту группу в настройках токена:

    | Вызываемая модель                 | Группа для включения      |
    | --------------------------------- | ------------------------- |
    | `doubao-seedance-2-5-260628`      | `SeeDance2`               |
    | `doubao-seedance-2-0-260128`      | `SeeDance2`               |
    | `doubao-seedance-2-0-fast-260128` | `SeeDance2` или `SD2Fast` |
    | `doubao-seedance-2-0-mini-260615` | `SeeDance2` или `SD2Mini` |

    Один ключ с включённой `SeeDance2` обеспечивает доступ ко всем четырём моделям. Модель тарификации также должна быть Pay-as-you-go Priority или Pay-as-you-go — **токены Pay-per-request не могут выполнять маршрутизацию**.
  </Accordion>

  <Accordion title="Python requests выдаёт ошибки gzip или возвращает усечённые тела, не являющиеся JSON">
    Заголовок `content-encoding: gzip` шлюза не соответствует фактическому кодированию тела. Симптомы включают `ContentDecodingError`, усечённое тело, не являющееся JSON (например, начальный `{"` теряется, и вы получаете только `id":"cgt-xxx"}`), или периодические ошибки 400. Добавьте `"Accept-Encoding": "identity"` в заголовки запроса; curl и browser fetch не затронуты.
  </Accordion>

  <Accordion title="Почему моё видео содержит звук? Как его отключить?">
    Для `generate_audio` по умолчанию используется `true` (проверено): модель автоматически добавляет речь, звуковые эффекты и фоновую музыку. Для вывода без звука явно передайте `"generate_audio": false`.
  </Accordion>

  <Accordion title="Где находится URL видео и почему он перестаёт работать?">
    При успешном выполнении URL находится в `content.video_url` ответа на запрос проверки (**не на верхнем уровне**). Это подписанная ссылка, действительная около 24 часов, поэтому сразу скачайте видео и разместите его на своём хостинге. Саму задачу можно запрашивать по task\_id в течение 7 дней.
  </Accordion>

  <Accordion title="Какое значение имеет статус успешного выполнения?">
    Конечный автомат имеет состояния `queued → running → succeeded / failed / expired`. Состояние успешного выполнения — **`succeeded`**, а не `completed`. Это распространённая ошибка при миграции с других API для работы с видео.
  </Accordion>

  <Accordion title="Можно ли загружать фотографии реальных людей для преобразования изображения в видео?">
    Нет. Ни Seedance 2.5, ни семейство 2.0 не принимают эталонные изображения или видео с лицами реальных людей (ограничения безопасности контента на стороне провайдера). Возможные альтернативы: повторно использовать результат с лицом, созданный моделями Seedance за последние 30 дней; воспользоваться готовыми виртуальными аватарами платформы (идентификаторы `asset://`); или использовать лицензированные ресурсы с лицами.
  </Accordion>

  <Accordion title="Взимается ли дополнительная плата за библиотеку ресурсов?">
    Нет. Загрузка виртуальных аватаров, проверка личности реальных людей и остальные возможности приватной библиотеки ресурсов **бесплатны при использовании API Seedance 2.0 на APIYI — без ежегодной платы**. Официально эта возможность приобретается как отдельное дополнение: для клиентов без рамочного соглашения годовой контракт стоит шестизначную сумму в юанях CNY (Volcengine также продаёт её напрямую). Мы ценим долгосрочных пользователей, поэтому эта стоимость уже включена в цену нашего API; для клиентов, использующих SD2 API в обычных коммерческих объёмах, дополнительная плата не взимается. Инструкции по использованию приведены в разделе [Библиотека ресурсов](/ru/api-capabilities/seedance2/asset-library).
  </Accordion>

  <Accordion title="Взимается ли плата за неудачные или отклонённые запросы?">
    Отклонение параметров (HTTP 400) **не тарифицируется** (проверено). При отправке средства списываются предварительно, а окончательный расчёт выполняется после завершения, поэтому баланс кратковременно изменяется — сверяйте его с журналами вызовов.
  </Accordion>

  <Accordion title="Как оценить расход token? Портретная ориентация стоит дороже?">
    `tokens ≈ duration(s) × width × height × 24 / 1024`, точность подтверждена в пределах 0,1%. Для каждого соотношения сторон в тарифном уровне используется одинаковая площадь изображения (720p 16:9 и 9:16 стоят по 108 900 token за 5 секунд) — **альбомная, портретная и квадратная ориентации стоят одинаково**.
  </Accordion>

  <Accordion title="В семействе 2.0 — standard, fast или mini?">
    Цена и скорость располагаются в порядке mini \< fast \< standard (номинальные значения на платформе для 720p/5 с: примерно ¥3.16 / ¥5.08 / ¥6.35). **Выбирайте mini для пакетного производства и задач с ограниченным бюджетом** — цена примерно вдвое ниже стандартной, а генерация выполняется быстрее всего (по измерениям за 2026-07: около 1,5–2,5 минуты для 5 с при 720p). Выбирайте standard для максимальной детализации, а fast — как промежуточный вариант. И mini, и fast ограничены разрешением 720p — запрос 1080p возвращает ошибку параметра 400 (тарификация не выполняется).

    **Если вам достаточно 1080p, стандартная модель 2.0 уже его поддерживает** — обновляться только ради этого не нужно. Переходите на **2.5** для роликов длительностью 30 секунд, более 9 эталонных изображений, вывода в mov или редактирования и расширения видео (цена примерно в 1,5 раза выше стандартной модели, группа та же, что у семейства 2.0).
  </Accordion>

  <Accordion title="Что делает duration: -1?">
    Модель самостоятельно выбирает длительность (4–30 с в 2.5, 4–15 с в семействе 2.0), а тарификация выполняется по фактической длительности результата. Итоговая длительность возвращается в поле `duration` задачи.

    **Учтите, что `-1` используется в 2.5 по умолчанию** (в семействе 2.0 по умолчанию используется 5 секунд): если не указать `duration`, модель сама выберет длительность, и проверенный запрос 2.5 без указания длительности вернул ролик на 10 секунд — ровно вдвое дороже ролика на 5 секунд. Если важна предсказуемость стоимости, явно передавайте `duration`.
  </Accordion>

  <Accordion title="Поддерживается ли параметр frames для дробных значений секунд?">
    Нет. `frames` и `camera_fixed` — параметры Seedance 1.x, **которые не поддерживаются ни Seedance 2.5, ни серией Seedance 2.0**. Вместо них используйте `duration` с целым числом секунд.
  </Accordion>

  <Accordion title="Можно ли одновременно использовать первый и последний кадр, первый кадр и эталонные изображения?">
    Нет — это три **взаимоисключающих** режима: первый и последний кадры (2 изображения с обязательными ролями `first_frame`/`last_frame`), первый кадр (1 изображение) и мультимодальное преобразование эталона в видео (роль изображения `reference_image`). Чтобы приблизить сценарий «первый/последний кадр + эталон», используйте режим эталона и укажите кадр в промпте.

    Ограничения на эталонные данные различаются по поколениям: **2.5 принимает 30 изображений + 10 видео + 10 аудиоклипов, причём аудио может использоваться отдельно**; семейство 2.0 принимает 9 изображений + 3 видео + 3 аудиоклипа и требует как минимум 1 изображения или 1 видео в дополнение к любому аудио.
  </Accordion>

  <Accordion title="Есть ли ограничения на параллельные запросы или очереди?">
    Группа `SeeDance2` поддерживает большое количество параллельных запросов без постановки в очередь (в нашем тесте 15 одновременных задач запустились сразу). Для более крупных постоянных нагрузок обратитесь в отдел продаж.
  </Accordion>

  <Accordion title="Есть ли ограничения на промпты?">
    Ограничивайте промпты примерно 500 китайскими иероглифами или 1000 английскими словами — более длинные промпты снижают детализацию. Поддерживаются следующие языки: китайский, английский, японский, испанский, португальский и индонезийский. Описывайте объект, действие, движение камеры и освещение или стиль.
  </Accordion>
</AccordionGroup>

## Связанная документация

* [Справочник API и Playground для генерации видео](/ru/api-capabilities/seedance2/video-generation) — `POST /seedance/api/v3/contents/generations/tasks`
* [Рабочий процесс с приоритетом ресурсов](/ru/api-capabilities/seedance2/asset-first-workflow) — как сделать так, чтобы create-task возвращал ответ мгновенно, когда запрос содержит медиафайлы, и что делать после истечения времени ожидания
* [Генерация видео с помощью VEO 3.1](/ru/api-capabilities/veo-3-1-official/overview) — официальный видеоканал Google
* [Бонусы за пополнение](/ru/faq/recharge-promotions) — итоговая стоимость примерно соответствует стоимости официального канала
* [Руководство по API](/ru/api-manual) — общие правила вызова

<Info>
  Seedance — одно из немногих первоклассных семейств видеомоделей 2026 года, которое **по умолчанию генерирует синхронизированный звук**. В сочетании с одинаковой стоимостью для разных соотношений сторон и ограничением в 30 секунд у версии 2.5 это надёжный основной канал для производства материалов для коротких видео и электронной коммерции. Для сравнения альтернатив тот же token (при включённых дополнительных группах) позволяет напрямую вызывать Sora 2, VEO 3.1 и Wan2.7.
</Info>
