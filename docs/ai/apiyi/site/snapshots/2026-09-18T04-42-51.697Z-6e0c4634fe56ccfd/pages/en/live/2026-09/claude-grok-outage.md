> ## Documentation Index
> Fetch the complete documentation index at: https://docs.apiyi.com/llms.txt
> Use this file to discover all available pages before exploring further.

# Claude Models Temporarily Unavailable Due to a Provider-Side Incident; Grok Also Down; OpenAI and Gemini Unaffected

> Anthropic is experiencing a provider-side incident, so Claude models are temporarily unavailable; follow the official status page at status.claude.com for updates. Grok models are down at the same time. OpenAI models are running normally, with the OpenAI API official route largely normal and the Azure official route available as backup, and Gemini models are unaffected. We will post here once service is restored.

**2026/9/3 23:55 (UTC+8)** · Model Status · Anthropic / xAI / OpenAI / Google

⚠️ **Claude models are temporarily unavailable due to a provider-side incident, and Grok models are down at the same time**

Cause: Anthropic's service is currently experiencing an incident, so requests to all `claude-*` models fail or time out. This is a provider-side issue, not an APIYI gateway problem. Progress and the estimated recovery time are published on the official status page: `status.claude.com`. xAI's `grok-*` models are also interrupted, and we are following up.

Status of other routes, for reference:

* OpenAI models (`gpt-*`): the OpenAI API official route is largely normal, with the Azure official route available as backup; running normally
* Gemini models (`gemini-*`): unaffected, running normally

We will post an update here once service is restored. Thank you for your patience; we are monitoring continuously.

***

← [Back to Live Updates](/en/live) · 📚 [Monthly archive](/en/live/archive)
