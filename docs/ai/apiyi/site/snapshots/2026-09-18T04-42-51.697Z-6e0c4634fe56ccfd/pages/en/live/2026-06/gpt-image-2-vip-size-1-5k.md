> ## Documentation Index
> Fetch the complete documentation index at: https://docs.apiyi.com/llms.txt
> Use this file to discover all available pages before exploring further.

# gpt-image-2-vip follow-up · size still uncontrollable, resolution dropped to 1.5K

> Progress update: putting the dimensions in your prompt makes size a bit more controllable, but you still cannot control size precisely. The biggest issue with the codex reverse channel gpt-image-2-vip right now is the resolution dropping to about 1.5K (e.g. 1536 × 1024). For precise size control and high resolution, use the official-relay gpt-image-2, billed by tokens at the same price as OpenAI.

**2026/6/17 15:00 (UTC+8)** · Model Status · OpenAI

🖼️ **`gpt-image-2-vip` follow-up · `size` still uncontrollable, resolution dropped to 1.5K** —— Progress update: putting the dimensions into your prompt makes size a bit more controllable, but you still **cannot control `size` precisely**; the biggest issue with the codex reverse channel `gpt-image-2-vip` right now is the resolution dropping to about 1.5K (e.g. 1536 × 1024 px). For precise control over Size and high resolution, use the official-relay `gpt-image-2` — billed by tokens at the same price as OpenAI's official site, with discounts reflected in top-up promotions.

***

← [Back to Live Updates](/en/live) · 📚 [Archive by month](/en/live/archive)
